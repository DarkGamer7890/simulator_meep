name: pmp
channels:
  - conda-forge

dependencies:
  - python=3.10

  # Meep with MPI support
  - pymeep=*=mpi_mpich_*
  - mpich

  # Common scientific libs
  - numpy
  - h5py


  # Pip-only packages
  - pip
  - pip:
      - pyvista
      - pyvistaqt 
      - vtk
      - pyqtgraph
